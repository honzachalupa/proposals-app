import React from 'react';
import Layout from 'Layouts/Main';
import SignUpForm from 'Components/SignUpForm';

export default () => (
    <section>
        <Layout>
            <h1>Sign Up</h1>

            <SignUpForm />
        </Layout>
    </section>
);
