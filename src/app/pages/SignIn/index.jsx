import React from 'react';
import Layout from 'Layouts/Main';
import SignInForm from 'Components/SignInForm';

export default () => (
    <section>
        <Layout>
            <h1>Sign In</h1>

            <SignInForm />
        </Layout>
    </section>
);
